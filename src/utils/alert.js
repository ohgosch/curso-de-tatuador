import styled from "styled-components"

import { COLORS } from "./constants"

export const Alert = styled.span`
  font-style: normal;
  font-weight: normal;
  font-size: 18px;
  line-height: 24px;
  color: ${COLORS.brand};
`
